#include<stdio.h>
/**
 * main - Entry point
 * I sometimes suffer from insomnia
 * Return: 0
 */
int main(void)
{
	char alph
		;
	for (alph = 'a'; alph <= 'z'; alph++)
	{
		putchar(alph);
	}
	putchar(\n);
	return (0);
}
